function y=BES_hankel_series_GAT()
%% CALL: y=BES_hankel_series_GAT()
%% convert series \sum_{\gamma\in S}A(\gamma R),
%% where R is dist from (x,y)//(r,\theta) to 
%% (x',y')//(r',\theta'),
%% to global polar coordinates using Graf's addition theorem:
%% H_0(kR)=\sum_{m=-\infty} ^\infty H_n(kr)J_n(kr')e^{in(\theta-\theta')}
%% where r'<r.
%% this gives the expression for 